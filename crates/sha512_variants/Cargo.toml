[package]
name = "sha512_variants"
version = "0.0.0"
publish = false
description = "Experimental optimised SHA-512 (K+W precombine + unchecked indexing)"
edition.workspace = true
authors.workspace = true
license.workspace = true

[lib]
name = "sha512_variants"
path = "src/lib.rs"

[dev-dependencies]
sha2 = "0.10.8"
